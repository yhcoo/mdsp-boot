spring:
  application:
    name: mdsp-auth-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: AUTH_GROUP
  profiles:
    active: dev